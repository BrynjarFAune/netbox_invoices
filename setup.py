from setuptools import find_packages, setup

setup(
    name='netbox-invoices',
    version='0.1',
    description='Prototype of netbox invoice plugin',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
